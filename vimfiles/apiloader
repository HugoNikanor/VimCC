local apiPath = "/utils/vimfiles/"
local apis = {
	"config",
	"global",
	"command",
	"screen",
	"vimode",
	"file",
	"logger",
}
for i=1, #apis do
	local test = os.loadAPI(apiPath..apis[i])
	if not test then
		error(apis[i])
	end
end
