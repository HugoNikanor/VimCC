
if not fs.isDir("/log") then
	fs.makeDir("/log")
end
local filename = "/log/.vimlog-"..os.day().."-"..os.time()
local file = fs.open(filename, "a")

function log( message )
	file.writeLine(message)
	file.flush()
end
